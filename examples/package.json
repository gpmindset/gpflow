{
    "name": "examples",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -b",
        "dev:example": "node dist/aws-ec2-example.js"
    },
    "dependencies": {
        "@gpflow/workflow": "workspace:*"
    },
    "devDependencies": {
        "@gpflow/typescript-config": "workspace:*"
    }
}